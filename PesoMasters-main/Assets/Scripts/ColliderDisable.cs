/*using UnityEngine;

public class Cp;  : MonoBehaviour
{
    // Flag to track whether the collider has been triggered
    private bool hasCollided = false;

private void OnTriggerEnter(Collider other)
{
    // Check if the collider has already been triggered
    if (hasCollided)
    {
        return;
    }

    // Add your logic or actions that should happen only once here

    // For demonstration purposes, let's print a message to the console
    Debug.Log("Collider has been triggered!");

    // Set the flag to true to indicate that the collider has been triggered
    hasCollided = true;
}
}*/